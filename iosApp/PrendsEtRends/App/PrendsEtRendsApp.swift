import SwiftUI

@main
struct PrendsEtRendsApp: App {
    var body: some Scene {
        WindowGroup {
            LoansDashboardView()
                .accentColor(Color(red: 0.18, green: 0.49, blue: 0.20))
        }
    }
}
